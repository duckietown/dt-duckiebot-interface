default:
  restart: unless-stopped
  network_mode: host
  privileged: true
  environment:
    DT_SUPERUSER: 1
  volumes:
    # dtps sockets
    - /data/ramdisk/dtps:/dtps
    # nvargus socket
    - /tmp:/tmp
