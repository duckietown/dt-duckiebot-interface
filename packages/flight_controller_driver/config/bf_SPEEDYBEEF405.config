#################################################################################################
# This file contains the diff configuration (i.e. the parameters changed from the default ones)
# of Betaflight for the SPEEDYBEEF405V3 board.
# To set the flight controller to these settings simply paste it into the prompt of the `CLI` tab
# of Betaflight Configurator
#################################################################################################

# diff

# version
# Betaflight / SPEEDYBEEF405V3 (SBF4) 4.3.2 Apr  7 2023 / 03:15:27 (f156481e9) MSP API: 1.44

# start the command batch
batch start

board_name SPEEDYBEEF405V3

# feature
feature -RX_SERIAL
feature -TELEMETRY
feature RX_MSP
feature LED_STRIP

# serial
serial 1 0 115200 57600 0 115200
serial 4 0 115200 57600 0 115200

# map
map AERT1234

# led
led 0 0,0::C:4
led 1 1,0::C:12
led 2 2,0::C:10
led 3 3,0::C:2

# aux
aux 0 0 0 1500 2100 0 0
aux 1 1 1 1500 2100 0 0
aux 2 15 0 1800 2100 0 0

# master
set acc_calibration = 4,-50,28,1
set blackbox_sample_rate = 1/16
set debug_mode = SCHEDULER_DETERMINISM

profile 0

rateprofile 0

# end the command batch
batch end

# Save the configuration to the board
save

# 